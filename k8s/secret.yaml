apiVersion: v1
kind: Secret
metadata:
  name: bravo-secrets
  labels:
    app: bravo-app
type: Opaque
stringData:
  # NOTE: example values for local/dev clusters only. In a real environment
  # generate these out-of-band (e.g. `mix phx.gen.secret`) and inject them via
  # a sealed secret / external secrets manager — never commit real secrets.
  #
  # The password (@dm1nC3nt1n3l4!) is URL-encoded here because it contains
  # characters that are reserved in a connection URL: @ -> %40, ! -> %21.
  database-url: "ecto://centinela_admin:%40dm1nC3nt1n3l4%21@postgres-service:5432/bravo_prod"
  secret-key-base: "F2k0Yq6mJpVb3nW8cR1tZ5xH7sLgD4eA9uQwErTyUiOpAsDfGhJkLzXcVbNmQwEr"
